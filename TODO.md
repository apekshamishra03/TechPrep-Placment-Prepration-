# TechPrep GitHub Deployment TODO

## Plan approved: Deploy to GitHub repo `techPrep` under `apekshamishra03` (public)

**Completed Steps:**
- [x] 1. Install GitHub CLI (gh) - installed, PATH may need refresh/restart
- [x] 2. Create .gitignore
- [x] 3. Create README.md
- [x] 4. git init
- [ ] 5. git add .
- [ ] 6. git commit -m "Initial commit"
- [ ] 7. gh repo create techPrep --public --push --source=.
- [ ] 8. Verify push (git remote -v; gh repo view)

**Notes:**
- gh not installed (confirmed).
- Exclude build/, venv*, *.db.
- Java backend optional.

Next: Install gh.
